export const AUTOPLAY_MS = 5000

// The design's artwork is a cutout that blends in; mock artwork has a hard edge, so this gradient
// stands in for the blend. Ours, not measured geometry, hence not in theme/tokens.ts.
export const ARTWORK_FADE_WIDTH = 72
